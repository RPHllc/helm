apiVersion: v1
kind: ConfigMap
metadata:
  name: coredns-custom
  namespace: kube-system
  labels:
    {{- include "coredns-custom.labels" . | nindent 4 }}
data:
  {{- range .Values.customServers }}
  {{ .filename }}: |
    {{- .content | nindent 4 }}
  {{- end }}
